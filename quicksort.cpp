#include <iostream>
#include <vector>
#include <algorithm>

using namespace std; 

// add randomizer might be O(n log n) or get the median pivot guarantee O(n log n ) of quicksort but more computaiton
// quicksort is like doing log n times binary search, each partition takes O(n)




int main(){
	vector<int> v {1, 3,4,2,0}; 


	return 0;
}


